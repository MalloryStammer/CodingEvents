<h1>Coding Events</h1>

<p>
    <a asp-controller="Events" asp-action="Add">Add Event</a>
</p>

@if (ViewBag.events.Count == 0)
{
    <p>No events yet!</p>
}

@*<ul>
        @foreach (string meeting in ViewBag.events)
         {
            <li>@meeting</li>
         }
    </ul>*@

@*<ul>
        @foreach (KeyValuePair<string,string> kvp in ViewBag.events)
         {
            <li>@kvp.Key: @kvp.Value</li> 
         }
</ul>*@

@* make name column and description column
    *@

<table>
    <thead> 
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var evt in ViewBag.events)
        {
        <tr>
            <td>@evt.Name</td>
            <td>@evt.Description</td>
            <td>@evt.Date</td>
        </tr>
        }
    </tbody>
</table>


